# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/

# C build artifacts (MSVC output from NetRedirector/build_dll.bat)
/NetRedirector/*.obj
/NetRedirector/NetRedirector.exp
/NetRedirector/NetRedirector.lib
/NetRedirector/NetRedirector.dll
/NetRedirector.dll

# C 測試產物 (NetRedirector/tests/run_tests.ps1 產出)
/NetRedirector/tests/*.exe
/NetRedirector/tests/*.obj
/NetRedirector/tests/*.lib
/NetRedirector/tests/*.exp

# Nuitka build output
/IntegratedApp.build/
/IntegratedApp.dist/
/IntegratedApp.dist.*/
/IntegratedApp.onefile-build/
/IntegratedApp.onefile-dist/

# Release packages (old zips live in releases/)
/releases/
NetRedirector-v*-win64.zip
NetRedirector2-v*-win64.zip

# Nuitka crash reports / misc junk
nuitka-crash-report.xml
query

# 使用者本機設定 (含個人代理組態與 DPAPI 加密密碼,不得進版控或發佈包)
# 範本請見 config.example.json
config.json
config.json.tmp
config.json.corrupt.bak

# pytest
.pytest_cache/

# Nuitka launcher byproducts (repo root)
/IntegratedApp.exe
/IntegratedApp.cmd

# 關閉流程診斷計時檔 (IntegratedApp.py 關閉時寫入)
/shutdown_timing.log
